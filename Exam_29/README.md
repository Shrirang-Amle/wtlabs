# React Digital Clock

Simple React lab exam program that shows a real-time digital clock using `useState()` and `useEffect()`.

## Run the project

```bash
npm install
npm run dev
```

## Features

- Functional React component
- Real-time clock update every second
- Time displayed in `HH:MM:SS` format
- Start/Stop button for clock control
